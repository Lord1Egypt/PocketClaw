# H1.5 — Canonical build and F-Droid cleanup

> **RECONSTRUCTED OPERATING RECORD — not the original prompt.** Rebuilt from Git
> history, `docs/FDROID_RELEASE.md`, tests, and commit evidence.

- **Status:** CLOSED, 2026-09-10.
- **Starting commit:** `492759bceca159f624d6f74810cbe10d6ad94124`.
- **Ending commit:** `6f117d38000bdba4f70cc7c87740494ba80727fc`.
- **Purpose:** Make the one canonical build suitable for Direct APK, Google
  Play, and Official F-Droid; remove packaged proprietary dependencies and
  runtime font fetching; prove Managed Runtime source builds.

## Reconstructed scope and invariants

- Remove Firebase/GMS from the canonical artifact instead of hiding them behind
  a flavor or scan exception.
- Bundle the exact Inter and Fira Code weights and license texts.
- Rebuild all eight Managed Runtime payloads from pinned source with committed
  payloads quarantined.
- Do not adopt changed payloads or rebuild Core without separate authorization.
- Keep vc62 and baseline 62 accepted; build only local, non-releasable evidence.

## Outcome and defects

`612ce96` removed Firebase/GMS and made the analytics provider default to none.
`6f117d3` bundled fonts and demonstrated source builds for all eight runtime
tools. Six rebuilt byte-identically.

Two directly related defects were found and fixed in recipes:

- the `gh` Android DNS vendoring guard no longer allowed its new
  `canonicalenv` dependency;
- Python's appended standard-library ZIP used wall-clock timestamps.

The corrected `gh` and Python outputs deliberately remained unadopted at this
phase boundary because adoption changed manifest checksums and required a Core
rebuild.

- **Next authorized milestone at closeout:** H1.5D source-built runtime
  adoption.
