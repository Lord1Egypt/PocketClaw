# H1.5D — Source-built runtime adoption

> **RECONSTRUCTED OPERATING RECORD — not the original prompt.** Rebuilt from Git
> history, `docs/FDROID_RELEASE.md`, Core provenance tests, and commit evidence.

- **Status:** CLOSED, 2026-09-10.
- **Starting commit:** `6f117d38000bdba4f70cc7c87740494ba80727fc`.
- **Ending commit:** `fb38c7d3c3fd31420c45a1977e1c1425ede3a378`.
- **Purpose:** Adopt the corrected source-built `gh` and Python payloads, update
  the pinned catalog, rebuild the Core pair required by that catalog change,
  and close the resolved F-Droid pending state.

## Reconstructed scope and invariants

- Change only the two authorized runtime payloads and their manifest checksums.
- Rebuild both Core binaries through the canonical path because the manifest is
  a fingerprinted, embedded Core input.
- Prove source-built output, committed payload, manifest digest, staged Core,
  and packaged artifact agree.
- Preserve version `0.2.0+62`, baseline 62, vc62 accepted state, and all release
  and device state.

## Outcome and verification

- `51ed822` adopted the two runtime payloads and checksum changes.
- `e62d67c` rebuilt and restaged both Core binaries.
- `dea826c` recorded adoption and removed the resolved gate note.
- `fb38c7d` aligned the signing/F-Droid contract test with the resolved state.

The Core fingerprint moved from `6e2382ae…` to
`876b87f5950452ba903301b6cce4cc96502ab4ff25d90d13e31b9da537e24b44`
because the embedded runtime catalog changed. vc62 remained the accepted
physical baseline; the H1.5D artifact was not installed or accepted.

- **Next authorized milestone at closeout:** Create/verify safety checkpoints,
  then H2 signer enrollment.
